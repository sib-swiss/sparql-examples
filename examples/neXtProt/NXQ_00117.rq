#+ endpoint: https://sparql.nextprot.org/sparql
#+ summary: Proteins with at least one sequence variant
#+ tags:
#+ - tutorial
#+ - variant
PREFIX : <http://nextprot.org/rdf/>

SELECT DISTINCT ?entry WHERE {
 ?entry :isoform ?iso.
 ?iso :variant ?var.
}
