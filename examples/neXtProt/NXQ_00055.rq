#+ endpoint: https://sparql.nextprot.org/sparql
#+ summary: Proteins which have genes of length greater than 2 million bp
#+ tags:
#+ - gene
#+ - tutorial
PREFIX : <http://nextprot.org/rdf/>

SELECT DISTINCT ?entry WHERE {
 ?entry :gene / :length ?l.
 filter (?l > 2000000)
}
