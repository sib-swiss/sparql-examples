#+ endpoint: https://sparql.omabrowser.org/sparql
#+ summary: Which species are available on OMA database and their scientific names?
PREFIX up: <http://purl.uniprot.org/core/>

SELECT ?species ?sciname
WHERE {
    ?species a up:Taxon ;
        up:scientificName ?sciname ;
        up:rank up:Species .
}
